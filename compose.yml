services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: my_blog
      POSTGRES_PASSWORD: password
      POSTGRES_DB: my_blog_development
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
